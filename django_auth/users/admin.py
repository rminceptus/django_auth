from django.contrib import admin
from django.contrib.auth.models import User

# The User model is already registered with Django's admin by default.
# Register any other custom models you create for the users app here.
